import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "BlueRoute Logistics | Connecting Businesses. Delivering Trust.",
  description:
    "BlueRoute Logistics – India's trusted multi-office logistics management platform for road, train and air freight.",
  keywords: "logistics, cargo, freight, shipment, India, delivery",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster />
      </body>
    </html>
  );
}
